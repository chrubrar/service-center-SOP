#!/bin/bash
# Script to start the API server with the correct environment settings

# Make the script executable
chmod +x set_env.sh

# Start the API server using the set_env.sh script
./set_env.sh python api.py
